using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TwitchIntegration;

public class AuthenticateUser : TwitchMonoBehaviour
{
    public bool authenticate = true;

    void Start()
    {
        //if(authenticate) TwitchManager.Authenticate("infinityvy", "infinityvy");
    }
}
